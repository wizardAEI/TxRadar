{
  "name": "txradar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "find public server -name '*.js' -print | sort | xargs -n1 node --check && node --check server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
